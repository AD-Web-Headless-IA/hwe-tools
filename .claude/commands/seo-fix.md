---
description: Apply SEO fixes from the last audit report to a site. Run /seo-audit {slug} first if no report exists.
allowed-tools: Read Write Edit Glob Grep Bash(git add *) Bash(git commit *) Bash(git status *) Bash(git diff *) Bash(git log *) Bash(pnpm *) Bash(ls *) Bash(find *) Bash(node --version)
argument-hint: [site-slug]
---

Invoke the `seo-fix` skill with site slug: $ARGUMENTS

Load `.claude/skills/seo-fix/SKILL.md` and follow every step exactly.
