---
description: Scaffold a new page in a site app — creates page.tsx with SEO metadata + empty composition, updates sitemap and navbar. Run /add-block afterwards to populate with blocks.
allowed-tools: Read Write Edit Glob Grep Bash(pnpm *) Bash(mkdir *) Bash(test *)
argument-hint: [site-slug] [page-slug]
---

Invoke the `create-page` skill with arguments: $ARGUMENTS

Load `.claude/skills/create-page/SKILL.md` and follow every step exactly.
