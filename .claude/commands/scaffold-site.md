---
description: Configure a client repo cloned from hwp-template — creates block re-exports, registry, theme, globals.css, client.config.ts, and all config files (DEC-011 + DEC-015 + DEC-017).
allowed-tools: Read Write Edit Glob Grep Bash(mkdir *) Bash(ls *) Bash(test *)
argument-hint: <slug> [--client-name "Display Name"]
---

Invoke the `scaffold-site` skill with arguments: $ARGUMENTS

Load `.claude/skills/scaffold-site/SKILL.md` and follow every step exactly.
