---
description: Add a block with realistic fake content to an existing page composition. Run /create-page first if the composition does not exist. BlockType is the stem without "Block" suffix (e.g. Hero, MediaText, Amenities).
allowed-tools: Read Write Edit Glob Grep Bash(pnpm *)
argument-hint: [site-slug] [page-slug] [BlockType]
---

Invoke the `add-block` skill with arguments: $ARGUMENTS

Load `.claude/skills/add-block/SKILL.md` and follow every step exactly.
