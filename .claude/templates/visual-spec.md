# Visual spec — {BlockName} for {Client Name}

> DRAFT — requires human review before implementation.
> Generated by `/design-block` on {date}.
> Source: `docs/design-language.md` + `src/theme/tokens.json` + existing blocks.
> After approval, feed this into `/propose` to start the SPECBOOT cycle.

## Purpose

{One paragraph: what this block does, where it appears on the site, what user need it addresses.}

## Layout

- **Structure:** {e.g. 2-col grid with image left, text right / full-width stack / card grid}
- **Max width:** {container-max from tokens or full-bleed}
- **Vertical padding:** {section-y from tokens or custom}
- **Mobile:** {how layout collapses on <768px}

## Typography

- **Eyebrow:** {yes/no — text size, color, tracking}
- **Heading:** {h2/h3 — font-heading or font-body — text size}
- **Body:** {font-body — text size — text-muted-foreground or text-foreground}
- **Pattern:** {eyebrow → h2 → body | h2 → body | ...}

## Imagery

- **Type:** {hero image | card thumbnails | icon illustrations | none}
- **Aspect ratio:** {16/9 | 4/3 | 1/1 | auto}
- **Treatment:** {full-bleed | contained with rounded | with overlay}
- **Overlay:** {none | bg-black/50 | gradient}

## CTA

- **Primary:** {shape, variant — e.g. rounded-full, bg-primary, px-8 py-3}
- **Secondary:** {shape, variant — e.g. outline, border-primary, px-8 py-3}
- **Placement:** {below text | overlaid on image | end of section}

## Interactions

- **Hover — card:** {scale(1.02) + shadow | background change | border change | none}
- **Hover — CTA:** {bg-primary/90 | scale(1.01) | none}
- **Hover — links:** {underline + text-primary | none}
- **Transitions:** {duration-200 ease-in-out | duration-300 ease}
- **Animations:** {none | fade-in on scroll | slide-up on scroll}

## Tailwind class recommendations

```tsx
// Outer section
<section className="py-[--section-y] bg-background">

  // Container
  <div className="mx-auto max-w-[--container-max] px-4 sm:px-6 lg:px-8">

    // {Describe the inner layout structure with Tailwind classes}

  </div>
</section>
```

## Content schema fields needed

```ts
{
  // Required
  title: string;
  // Optional
  subtitle?: string;
  // {other fields inferred from the block's purpose}
}
```

## CVA variants needed

| Variant key | Values | Default | Purpose |
|---|---|---|---|
| {variant} | {a | b} | {a} | {Why it varies} |

## Design language alignment

- ✓ Section spacing matches `design-language.md §Layout patterns`
- ✓ Card style matches `design-language.md §Card style`
- ✓ Typography follows `design-language.md §Typography hierarchy`
- ✓ CTA style matches `design-language.md §Component patterns`
- (?) {Any unresolved design question that needs human decision}

## Open questions

- {Any decision that could not be made from existing docs alone}
